import React from "react";

export default async function BuyLinksWrapperServerSide(props) {

  // const res = await getRegionBuyLinks(props?.handheldInfo?.id, props?.buyLinksAuthor);
  // const region = res?.region;
  // const country = res?.country;

  // const hasBuyLinks = res?.results?.length > 0;

  // if (!hasBuyLinks){
  //   if (props?.noBuyLinksFallbackText && props?.asText) return props.noBuyLinksFallbackText;
  //   return 
  // }

  // return <BuyLinksAction {...props} region={region} country={country}/>;
  return <></>
}
